### Vector

    - Dynanic array of same type of elements
    - Syntax:
    	let name:Vec<data-type> = Vec::new(); // for empty vectors
    	let name:vec![e1,e2,e3];	      // vector with some elements e1,e2,e3
    - Example:
    	let v:Vec<i32> = Vec::new();
    	let v=vec![1,2,3,4];
